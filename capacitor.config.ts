import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jerfrim.appmobile',
  appName: 'app-mobile-hexagonal',
  webDir: 'www'
};

export default config;
